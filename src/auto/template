////// template for redux extention

//// Dispatcher template

// Arrange
const action = ${action}
const prevState = ${prevState}

//// Assertion Template

const expectedResult = ${curState}
// Act
const result = autoReducer(prevState, action);
// Assert
expect(result).toEqual(expectedResult)

//// Wrapping template

it('reducers', () => {
  ${assertions}
});